#include <bits/stdc++.h>

using namespace std;

int main(int argc, char** argv) {
    int N, X, Y;
    cin >> N >> X >> Y;

    // insert code here
    for(int j = 0; j < N; j++) {
        string name;
        int px, py;
        cin >> name >> px >> py;
    }

    return 0;
}